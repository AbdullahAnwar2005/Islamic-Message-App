import 'dart:ui';

class AppColors {
  // Light theme colors
  static const lightPrimary = Color(0xFF00695C); // Slightly deeper teal
  static const lightSecondary = Color(0xFF80CBC4);
  static const lightBackground = Color(0xFFF8F5F2); // Warm off-white
  static const lightCard = Color(0xFFFFFFFF); // Pure white for better contrast
  static const lightCardBoarder = Color(0xFFE0E0E0); // Subtle border
  static const lightTextPrimary = Color(0xFF263238); // Blue-grey dark
  static const lightTextSecondary = Color(0xFF546E7A);
  static const lightButton = Color(0xFF00796B);
  static const lightChipBackground = Color(
    0xFFECEFF1,
  ); // Light grey-blue for chips

  // Dark theme colors
  static const darkPrimary = Color(0xFF4DB6AC); // Balanced Teal
  static const darkSecondary = Color(0xFF004D40);
  static const darkBackground = Color(0xFF121212); // Material Dark Background
  static const darkCard = Color(0xFF1E1E1E); // Elevated Dark Surface
  static const darkCardBoarder = Color(0xFF2C2C2C); // Subtle border
  static const darkTextPrimary = Color(0xFFFFFFFF); // Clear White
  static const darkTextSecondary = Color(0xFFB0BEC5); // Blue-grey light
  static const darkButton = Color(0xFF4DB6AC);
  static const darkChipBackground = Color(0xFF2C2C2C);
}
